# Core lab dependencies.
# Note: sentence-transformers pulls in torch, but it is only imported lazily when
# a real HuggingFace embedding run is requested. Mock-based tests never touch it.

llama-index>=0.11
llama-index-embeddings-huggingface>=0.3
sentence-transformers>=2.2
langchain-ollama>=0.1
pydantic>=2
pyyaml>=6
pytest>=7
