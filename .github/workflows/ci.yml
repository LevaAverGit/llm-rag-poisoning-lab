name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read
  security-events: write

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install dependencies
        # Pull the CPU-only torch first so the sentence-transformers dependency
        # does not drag in the multi-GB CUDA build on the runner.
        run: |
          pip install torch --index-url https://download.pytorch.org/whl/cpu
          pip install -r requirements.txt
      - name: Run the offline suite
        # Deterministic MockEmbedding + mock LLM; torch is never imported here.
        run: python -m pytest -q
      - name: Lint (non-blocking)
        run: |
          pip install ruff
          ruff check . || true

  security:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install security tools
        run: pip install "bandit[sarif]" pip-audit
      - name: Bandit SAST (SARIF)
        run: bandit -r . -x ./tests -f sarif -o bandit.sarif || true
      - name: Upload Bandit SARIF
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: bandit.sarif
          category: bandit
        continue-on-error: true
      - name: Dependency audit (non-blocking)
        run: pip-audit -r requirements.txt || true
